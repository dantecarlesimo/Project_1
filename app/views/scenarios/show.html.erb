<ul class="nav nav-pills" id="blue">
  <li id="scenario_title"><%=@scenario.title%></li>
  <li><%= link_to('Edit Scenario', edit_scenario_path(@scenario)) %></li>
  <li><%= link_to('Delete Scenario', @scenario, :method => :delete, :confirm => 'Are you sure?') %></li>
  <div class='spacer'></div>
</ul>

<div class="row">
  <div class="col-md-5 col-sm-5">
 

<!-- <h3><%=current_user.first_name%> <%=current_user.last_name%></h3> -->
<h3>Borrower's Information</h3>
    <p class="lead">Income:<%= @scenario.income%></p>
    <p class="lead">Current Housing Expense:<%= @scenario.housing_exp%></p>
    <p class="lead">Other Monthly Expenses:<%= @scenario.other_exp%></p>
    <p class="lead">Credit Score:<%= @scenario.credit_score%></p><br><br>

<h3>Property Information</h3>
    <p class="lead">Home Address: <%= @scenario.title%></p>
    <p class="lead">Home Purchase Price: $<%= @scenario.purchase_price%></p>
    <p class="lead">Mortgage Loan Amount: $<%= @scenario.loan_amount%></p><br><br>
    
<h3>Mortgage Loan Options</h3>
    <h4 class="lead">30 Year Fixed</h4>
    <p class="lead">Today's Rate:<%= @thirty_year_fixed%>%</p>
    <p class="lead">Monthly Payment: $<%= @monthly_payment%></p>
    <p class="lead">Debt to Income Ratio (DTI): <%= @dti %>%</p>
    <p class="lead">Change in Disposable Income: $<%= @disp_income_diff%></p><br><br>


    <h4 class="lead">15 Year Fixed</h4>
    <p class="lead">Today's Rate:<%= @fifteen_year_fixed%>%</p>
    <p class="lead">Monthly Payment: $<%= @monthly_payment_2%></p>
    <p class="lead">Debt to Income Ratio (DTI): <%= @dti_2 %>%</p>
    <p class="lead">Change in Disposable Income: $<%= @disp_income_diff_2%></p><br><br>

    <h4 class="lead">5/1 Adjustable Rate</h4>
    <p class="lead">Today's Rate:<%= @five_one_adjust%>%</p>
    <p class="lead">Monthly Payment: $<%= @monthly_payment_3%></p>
    <p class="lead">Debt to Income Ratio (DTI): <%= @dti_3 %>%</p>
    <p class="lead">Change in Disposable Income: $<%= @disp_income_diff_3%></p><br><br>










  </div>
 </div>
<%#= render :partial => 'details' %>


